import {
    filterControl,
    uiSlider,
    filterBudget,
    filterDropdownChoice
} from './components/filter';
document.addEventListener('DOMContentLoaded', () => {
    filterControl();
    uiSlider();
    filterBudget();
    filterDropdownChoice();
})
